import { bubbleSortCourseSeed } from "./bubbleSortCourseSeed";
import type { CourseSeed } from "./learningSeedTypes";

export const learningSeed: { courses: CourseSeed[] } = {
  courses: [bubbleSortCourseSeed],
};
